# evaluation

Public eval fixtures and contracts, carried forward/extended from V1's `evaluation/`
(EVALUATION_PLAN.md, 15 public fixtures, JSON response contracts covering the 12 required
categories). Referenced from the V1 sibling directory today; not yet locally copied (see
`docs/product/state/current/current_state_assessment.md` NAB-3). Stage 14 (eval-ai-cache)
is the primary consumer.
