# hooks

Lifecycle hooks (pre/post tool-use, session events). See [hooks.md](hooks.md) for the Stage 12
index (9 bindings) and [../skill_vs_hook_boundary.md](../skill_vs_hook_boundary.md) for the
rule deciding what belongs here vs. in `skills.md`. `../hooks.json`'s `hooks: {}` stays empty
until Stage 20 — see its comment.
