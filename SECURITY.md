# Security

AI-specific threat models live in `security/threat-models/`. Report issues per
`security/policies/`. No real PHI/PII is ever committed — synthetic data only,
per the non-negotiables in `README.md`.
