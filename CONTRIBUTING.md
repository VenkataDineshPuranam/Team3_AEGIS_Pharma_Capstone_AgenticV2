# Contributing

This repo follows Spec-Driven Development — see `SPEC_DRIVEN_DEVELOPMENT.md`.
One branch per stage (see `STAGES.md`). Write the spec before the artefact/code.
Every stage doc must include a DMAIC-Lean block (Define/Measure/Analyze/Improve/Control).
