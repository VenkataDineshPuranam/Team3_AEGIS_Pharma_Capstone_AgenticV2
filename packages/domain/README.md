# domain

Shared domain model code: bounded-context entities, value objects, ontology bindings.

Stage 13 design (no code yet — Stage 20 is deliberately last):
[docs/architecture/ontology/](../../docs/architecture/ontology/) — 17 classes, 14 edge types,
grounded against `knowledge/` and V1's actual fixture-data schema rather than DDD prose alone.
