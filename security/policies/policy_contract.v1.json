{
  "_note": "Stage 20a's versioned policy contract -- loaded by services/integration/policy_engine.py's policy_load (ADR-005, BC-3: fail closed if this file is missing/unreadable). Prohibition contracts here are ADR-004 layer 3 (runtime guard), the third of three independent layers -- layers 1 (schema) and 2 (tool capability) are enforced by packages/domain/payloads.py and the tool_contracts JSON schemas already having no disposition fields at all.",
  "version": "v1",
  "prohibition_contracts": {
    "batch_review": {
      "workflow": "batch_review",
      "banned_terms": [
        "release the batch", "batch is released", "recommend release",
        "reject the batch", "batch is rejected", "recommend rejection",
        "reprocess the batch", "recommend reprocessing",
        "relabel the batch", "recommend relabeling",
        "recall the batch", "recommend recall",
        "batch is approved for release", "cleared for release", "fit for release"
      ],
      "banned_field_names": [
        "release_recommended", "reject_recommended", "reprocess_recommended",
        "relabel_recommended", "recall_recommended", "disposition"
      ]
    },
    "pv_intake": {
      "workflow": "pv_intake",
      "banned_terms": [
        "causality is", "causally related", "confirmed causality",
        "seriousness is", "this case is serious", "not serious",
        "expected per label", "unexpected per label", "expectedness is",
        "reportable to", "must be reported", "not reportable", "reportability is",
        "signal confirmed", "confirmed signal", "this is a validated signal"
      ],
      "banned_field_names": [
        "causality", "seriousness", "expectedness", "reportability", "signal_confirmed"
      ]
    },
    "supply_planning": {
      "workflow": "supply_planning",
      "banned_terms": [
        "allocate", "allocated to", "reserve for", "reserved for",
        "ship to", "shipment authorized", "release authorization",
        "recall scope", "execute this option", "proceed with allocation"
      ],
      "banned_field_names": [
        "allocated_quantity", "reserved_for", "ship_to", "release_authorization", "recall_scope"
      ]
    },
    "research_review": {
      "workflow": "research_review",
      "banned_terms": [
        "model is qualified", "qualified for portfolio use", "intended use is approved",
        "target is validated", "confirmed target", "validated target",
        "recommend advancing", "recommend qualification"
      ],
      "banned_field_names": [
        "model_qualified", "intended_use_approved", "target_validated", "disposition"
      ]
    },
    "clinical_integrity": {
      "workflow": "clinical_integrity",
      "banned_terms": [
        "protocol deviation is acceptable", "deviation disposition", "unblind the",
        "recommend unblinding", "participant is eligible", "participant is ineligible",
        "eligibility determination", "recommend enrollment", "recommend discontinuation"
      ],
      "banned_field_names": [
        "protocol_deviation_disposition", "unblinding_action", "eligibility_final_determination", "disposition"
      ]
    },
    "regulatory_completeness": {
      "workflow": "regulatory_completeness",
      "banned_terms": [
        "is reportable", "not reportable", "variation is classified as",
        "submission is ready", "cleared for submission", "recommend filing",
        "regulatory determination is"
      ],
      "banned_field_names": [
        "variation_classification_final", "submission_ready", "regulatory_determination", "disposition"
      ]
    }
  }
}
